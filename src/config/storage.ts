// Placeholder for cloud storage setup (Cloudinary or Supabase)
export const uploadToCloud = async (filePath: string): Promise<string> => {
  // TODO: integrate Cloudinary or Supabase SDK
  return `https://fake-cloud-storage.com/${filePath}`;
};
